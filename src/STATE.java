/**
 * The STATE ENUM stores the different states a character can be in.
 * Note: not all states are used by all characters. E.g. CROUCH is only utilised by the player character.
 * @author Shane Birdsall
 */
public enum STATE {
	MOVE, SHOOT, KNIFE, CROUCH, IDLE;
}
